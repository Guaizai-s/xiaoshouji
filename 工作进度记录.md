# 小手机项目 - 工作进度记录




## 📅 日期：2026-03-13

---

## ✅ 已完成功能

### 核心功能
- ✅ 多角色AI聊天（支持Anthropic和OpenAI格式）
- ✅ 15轮智能上下文管理
- ✅ 图片识别（Claude Vision）
- ✅ 语音输入（Web Speech API，需VPN）
- ✅ 本地数据存储（IndexedDB + Dexie.js）
- ✅ 角色创建/编辑/删除
- ✅ 本地头像上传（角色和个人）
- ✅ 智能延迟回复（模拟打字）
- ✅ **表情过滤**：`[表情：xxx]` 自动显示表情图片
- ✅ **气泡分隔**：AI回复换行自动分割成多个气泡

### UI界面
- ✅ 高仿微信UI（去掉所有emoji，改用SVG图标）
- ✅ 底部导航栏（微信风格SVG图标）
- ✅ 四个Tab页面（微信/通讯录/发现/我）
- ✅ 聊天室界面
- ✅ 消息气泡组件（支持多气泡分隔）
- ✅ 分支模式导航（Tab切换不堆叠历史）
- ✅ 返回按钮（纯SVG箭头，无文字）

### 技术架构
- ✅ Vue 3 + Vite
- ✅ Vue Router
- ✅ Dexie.js（IndexedDB封装）
- ✅ PWA配置（Vite PWA Plugin）
- ✅ 本地代理服务器（解决开发环境CORS）
- ✅ **Vercel Serverless Function**（解决生产环境CORS）

---

## 🎉 2026-03-13 完成的工作

### Bug修复
1. ✅ **手机端发送失败** - 创建Vercel Serverless Function (`api/chat.js`)
2. ✅ **PWA无法安装** - 修复图标文件名（下划线改连字符）
3. ✅ **Chrome底部栏不显示** - 重置button默认样式，添加safe-area适配
4. ✅ **返回按钮触发Google搜索** - 删除"返回"文字，改为纯SVG箭头
5. ✅ **图片发送失败** - Vision API改为走后端代理
6. ✅ **头像不实时更新** - ChatRoom从localStorage读取用户头像
7. ✅ **API返回格式错误** - 改进错误处理，捕获非JSON响应

### 新功能
1. ✅ **表情过滤** - `[表情：亲亲]` 自动替换为 `/emojis/亲亲.png`
2. ✅ **气泡分隔** - AI回复中的换行(`\n`)自动分割成多个独立气泡

### 已上传的表情包（22个）
- 亲亲、送心、送花花、喜欢你、幸福、卖萌、得意、真棒
- 星星眼、委屈、哭哭、讨厌你、震惊、晕、了解、吃饭、等你
- 小猫同意、小猫反对、小猫思考、小猫道歉、呼叫宝宝

---

## 🔧 技术细节

### API调用策略（已更新）
- **开发环境**：使用本地代理服务器（localhost:3001）
- **生产环境**：使用Vercel Serverless Function（/api/chat）
- **自动判断**：根据hostname自动切换

### 关键文件
```
api/chat.js              # Vercel后端函数（解决CORS）
src/services/claude.js   # API调用（自动判断环境）
src/components/MessageBubble.vue  # 气泡组件（表情+分隔）
src/components/NavBar.vue         # 导航栏（纯箭头返回）
src/components/TabBar.vue         # 底部栏（Chrome兼容）
public/emojis/           # 表情图片目录
```

### 数据库结构（IndexedDB）
```javascript
roles: {
  id, name, avatar, apiKey, baseUrl, model, apiFormat, systemPrompt, createdAt, updatedAt
}

conversations: {
  id, roleId, lastMessage, unread, updatedAt
}

messages: {
  id, conversationId, role, content, type, timestamp
}
```

---

## 📂 项目文件位置

### 本地路径
```
D:\claude_project\小手机\
├── api/
│   └── chat.js          # Vercel Serverless Function
├── src/
│   ├── views/           # 页面组件
│   ├── components/      # 通用组件
│   ├── services/        # 服务（API、数据库、语音）
│   ├── router/          # 路由配置
│   └── assets/styles/   # 样式
├── public/
│   ├── emojis/          # 表情图片（22个）
│   ├── icon-192x192.png # PWA图标
│   └── icon-512x512.png # PWA图标
├── proxy-server.js      # 本地代理服务器
└── package.json
```

### 远程仓库
- **GitHub**：https://github.com/Guaizai-s/xiaoshouji
- **Vercel部署**：https://xiaoshouji-nine.vercel.app/

---

## 🚀 如何运行

### 本地开发
```bash
# 终端1：启动前端
cd "D:\claude_project\小手机"
npm run dev:host

# 终端2：启动代理服务器
cd "D:\claude_project\小手机"
npm run proxy

# 访问
电脑：http://localhost:3000
手机：http://192.168.43.241:3000
```

### 生产环境
直接访问：https://xiaoshouji-nine.vercel.app/
- 无需运行任何本地服务
- API请求通过Vercel Function代理

---

## ⚠️ 已知限制

1. **语音识别需要VPN** - Web Speech API依赖Google服务器
2. **API访问可能需要VPN** - 取决于API提供商的网络状况

---

## ✨ 项目亮点

1. **高仿微信UI** - 纯SVG图标，无emoji
2. **本地数据** - 完全离线可用（除API调用）
3. **15轮上下文** - 智能对话管理
4. **多模态** - 文字、图片、语音输入
5. **PWA支持** - 可安装到手机桌面
6. **响应式设计** - 移动端优先
7. **表情系统** - 支持自定义表情包
8. **智能气泡** - 自动分隔长消息

---

## 🎯 项目状态

- ✅ 功能完整的AI聊天应用
- ✅ 高仿微信的用户体验
- ✅ 可在手机上安装使用（PWA）
- ✅ 解决了移动端CORS问题
- ✅ 表情和气泡分隔功能

---

## 🎉 2026-03-19 完成的工作

### Bug修复
1. ✅ **聊天界面白屏** - 回滚MessageBubble.vue至稳定版本
2. ✅ **手机端发送按钮变形** - 添加`white-space: nowrap`和`flex-shrink: 0`
3. ✅ **顶部绿色状态栏** - `theme-color`改为`#EDEDED`

### 新功能
1. ✅ **气泡尖角** - 对方气泡左侧、用户气泡右侧添加三角形小尾巴
2. ✅ **Q弹入场动画** - 消息气泡出现时有弹跳效果
3. ✅ **删除语音按钮** - 移除语音输入功能（暂不开发），删除speech.js依赖
4. ✅ **加号按钮** - 左侧改为微信风格加号，点击可选择图片（后续扩展更多功能）
5. ✅ **换行气泡每条显示头像** - 多气泡分隔时每条都显示角色头像

### 未来规划（讨论）
- 仿桌面App首页
- 游戏（真心话、你画我猜、狼人杀等）
- 记账本、音乐、阅读（角色陪读）、学习空间、日历
- MiniMax语音TTS（角色发语音条）
- 美化功能（主题、壁纸等）
- System prompt分层：用户填写性格描述，系统自动拼接功能性prompt（表情包、语音规则等），不暴露给用户

---

---

## 🎉 2026-03-20 完成的工作

### 新功能
1. ✅ **iOS桌面首页** - 新建 `Desktop.vue`，支持壁纸/图标自定义上传，Dock 栏玻璃效果，4个应用（微信/游戏/账本/设置）
2. ✅ **路由调整** - `/` 指向桌面首页，微信图标跳 `/chats`

### Bug修复
1. ✅ **顶部绿色线条** - vite.config.js `theme_color` 改为 `#EDEDED`（PWA manifest）
2. ✅ **图片发送无上下文** - 存 base64 dataUrl 持久化，带文字上下文一起发给模型
3. ✅ **发送后键盘自动收起** - 改为点击非输入框区域才 blur
4. ✅ **气泡宽度不一致** - `width: 11em` 固定11字换行
5. ✅ **导出/导入数据** - 存储空间展开面板，导出 JSON / 导入覆盖
6. ✅ **正在输入提示** - 顶部 navbar 标题发送后切换为"对方正在输入…"
7. ✅ **气泡延迟动画** - 弹跳幅度加大（scale 0.7→1.06→1），多气泡每条间隔500ms依次出现

---

## 🎉 2026-03-21 完成的工作

### 新功能
1. ✅ **桌面重构** - `Desktop.vue` 重写，iOS风格两屏图标 + 小组件（电量、倒数日），长按进编辑模式可拖动，App图标全部换为SVG
2. ✅ **AppSettings.vue（桌面设置页）** - 新建，包含：壁纸/图标上传、API方案管理（可多套）、MiniMax语音API配置、数据导出（原Me页存储空间功能移至此处）
3. ✅ **ChatDetails.vue（聊天详情页）** - 新建，入口：聊天室右上角"…"，包含：创建群聊占位、API方案切换、MiniMax Voice ID + 语速配置、上下文长度选择、主动发送（定时/断联）、记忆设置（长期/核心）、置顶、消息免打扰
4. ✅ **Me.vue** - 设置入口留空（不跳转桌面设置），存储空间功能已迁移

### Bug修复
1. ✅ **Desktop.vue 500报错** - 移除动态`<style>`标签，改用`document.head`注入壁纸样式
2. ✅ **壁纸不持久化** - 改为base64存IndexedDB，刷新后恢复
3. ✅ **PNG壁纸不支持** - 修复文件类型检测，支持jpg/png/webp/gif
4. ✅ **Me.vue设置跳转到桌面设置** - 去掉router跳转，留空
5. ✅ **桌面光晕/震动特效** - 删除，保持简洁
6. ✅ **卸载App按钮位置远** - 直接删除卸载功能

### 关键文件更新
```
src/views/Desktop.vue       # 桌面首页（重写）
src/views/AppSettings.vue   # 桌面设置页（新建）
src/views/ChatDetails.vue   # 聊天详情页（新建）
src/views/Me.vue            # 我的页面（设置留空）
src/router/index.js         # 新增 /app-settings、/chat-details/:id 路由
src/services/db.js          # 新增 apiProfiles 表、roles扩展字段
```

### 数据库新增结构
```javascript
apiProfiles: {
  id, name, apiKey, baseUrl, model, apiFormat, createdAt
}
// roles 新增字段：
{
  apiProfileId,           // 关联的API方案
  voiceId, voiceSpeed,    // MiniMax语音
  contextLength,          // 上下文记忆长度
  activeSchedule,         // 主动发送配置 {type, interval}
  longMemory,             // 长期记忆（文本）
  coreMemory,             // 核心记忆（文本）
  isPinned,               // 置顶
  isMuted,                // 消息免打扰
}
```

### ⏳ 待实现
- 群聊功能（ChatDetails已有入口占位）
- 游戏App（占位中）
- 账本App（占位中）
- 主动发送消息逻辑（定时器/断联检测）
- MiniMax TTS实际调用（角色发语音条）

---

**记录时间**：2026-03-21

---

## 🎉 2026-03-22 完成的工作

### Bug修复
1. ✅ **气泡宽度固定不变** - `width: 11em` 改为 `width: auto + max-width: 12em`，短文本气泡不再撑满，长文本约12字处自动换行
2. ✅ **顶部导航栏残留绿色线** - Navbar `border-bottom` 颜色改为与背景一致（白色），消除视觉残留
3. ✅ **Chrome底部栏被挤压** - Home指示条移入Dock栏内部，Dock `margin-bottom` 从 `-28px` 改为 `12px`，避免超出视窗
4. ✅ **API额度不足错误** - 修复API调用链路断开的bug
   - `db.js`: 添加 `apiProfileService.getById()` 方法
   - `ChatRoom.vue`: 加载角色时获取并合并API方案配置
   - `AppSettings.vue`: API方案配置添加 `apiFormat` 选择下拉框
5. ✅ **URL智能补全bug** - 修复baseUrl末尾斜杠导致的URL拼接错误
6. ✅ **Chrome安全区域问题** - 将 `100vh` 改为 `100dvh`（动态视口高度），适配Chrome工具栏

### 修改文件
```
api/chat.js                   # URL智能补全优化
src/App.vue                   # 100vh → 100dvh
src/assets/styles/weixin.css  # 气泡宽度、Navbar边框、100dvh
src/components/NavBar.vue     # 添加safe-area-inset-top白色背景
src/services/db.js            # 添加getById方法
src/views/AppSettings.vue     # API方案添加apiFormat选择、100dvh
src/views/ChatDetails.vue     # 100vh → 100dvh
src/views/ChatRoom.vue       # 获取并合并API配置
src/views/Desktop.vue        # 安全区域、100dvh
```

### API调用链路修复说明
```
用户配置流程：
设置 → 聊天 API → 新建方案（填写 apiKey/baseUrl/model/apiFormat）
      → 创建/编辑角色 → 选择「API 方案」

聊天流程：
ChatRoom → 获取 role.apiProfileId
        → apiProfileService.getById(apiProfileId) 获取配置
        → 合并到 role 对象 { apiKey, baseUrl, model, apiFormat }
        → callClaude(role, messages) 调用成功
```

---

**记录时间**：2026-03-22

---

## 🎉 2026-03-23 完成的工作

### Bug修复
1. ✅ **图片上传413错误** - 添加图片压缩功能（最大宽度800px，质量0.7），图片消息自动限制上下文为5轮
2. ✅ **API需要代理** - 统一使用Vercel Function（`/api/chat`），无需本地代理
3. ✅ **设置页面返回按钮触发搜索** - 删除"聊天信息"文字，只保留箭头图标
4. ✅ **页面返回动画异常** - 删除所有页面切换动画（滑动、淡入淡出）
5. ✅ **新角色头像无法上传** - 修复缺失的 `avatarFile` 变量定义

### 功能优化
1. ✅ **动态上下文长度** - 文字消息使用用户设置的上下文长度（默认15轮），图片消息自动限制为5轮避免请求体过大
2. ✅ **图片压缩** - 新增 `compressImage()` 函数，自动压缩图片减小请求体积

### 修改文件
```
src/services/claude.js      # API统一走Vercel
src/views/ChatRoom.vue      # 图片压缩、动态上下文
src/views/ChatDetails.vue   # 返回按钮只保留箭头
src/views/NewRole.vue       # 修复头像上传
src/App.vue                 # 删除页面切换动画
```

---

**记录时间**：2026-03-23
## 📅 日期：2026-03-24

### ✅ 今日完成
1. **UI优化**：删除桌面底部模拟导航栏横条
2. **聊天功能增强**：改造聊天功能键为半页弹出层（图片、红包、礼物、转账、表情包）
3. **交互优化**：在发送按钮左侧添加生成按钮，用户可手动触发AI回复
4. **时间感知**：实现真实时间感知功能，开启后每次生成时将当前时间发送给模型
5. **个性化**：角色详情页添加聊天背景图更换功能
6. **功能整合**：将角色卡编辑功能迁入角色详情页，删除通讯录编辑入口
7. **UI增强**：聊天界面右上角添加爱心按钮（预留角色心声功能）

---## 📅 日期：2026-03-25

### ✅ 今日完成（上午）
1. **Bug修复**：修复ChatRoom.vue中computed未导入导致页面白屏的问题
2. **UI修复**：修复NavBar按钮位置，确保爱心和详情按钮显示在右上角
3. **功能面板重构**：将聊天功能键改造为微信风格的固定高度面板（220px）
4. **生成按钮**：在发送按钮左侧添加生成按钮，用户可手动触发AI回复
5. **图标风格统一**：采用微信风格，白色圆角方块背景+黑色线条图标

### ✅ 今日完成（下午）
1. **用户人设卡功能**：新增UserPersonas.vue页面，支持创建/编辑/删除多个用户人设卡
2. **表情包管理功能**：新增StickerManage.vue页面，支持上传表情包图片并添加描述
3. **角色详情页增强**：ChatDetails.vue新增人设卡选择和表情包关联功能
4. **AI对话增强**：ChatRoom.vue自动将选中的人设信息和表情包列表发送给AI
5. **表情包过滤重构**：MessageBubble.vue改为从数据库动态加载表情包，替代硬编码的/emojis/路径
6. **夜间模式**：AppSettings.vue新增夜间模式开关，全局CSS支持dark theme
7. **数据库扩展**：新增userPersonas和stickers表，支持人设和表情包数据持久化
8. **导航更新**：Me.vue新增"用户设定"和"表情包上传"入口

### 技术细节
- 数据库版本升级至v4，新增userPersonas和stickers表
- 表情包格式：`[表情:名字]` 自动匹配数据库中的imageUrl
- 人设信息通过系统提示词发送：`[用户信息]\n{description}\n[用户状态]\n{status}`
- 表情包提示词：`你可以使用以下表情包，使用格式为[表情:名字]:{描述列表}`
- 夜间模式通过`data-theme="dark"`属性切换CSS变量

---
